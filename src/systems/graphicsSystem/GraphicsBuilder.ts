import type { GraphicsManager } from "./GraphicsManager";


export class GraphicsBuilder {
	static instance: GraphicsBuilder;

	Square(x:number, y:number, width: number, height: number, GraphicsManager: GraphicsManager){

	}

	Triangle(x:number, y:number, width: number, height: number, GraphicsManager: GraphicsManager){
		
	}
	Circle(x:number, y:number, width: number, height: number, GraphicsManager: GraphicsManager){
		
	}

}